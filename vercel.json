{
  "version": 2,
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ],
  "functions": {
    "api/index.py": {
      "runtime": "python3.11"
    }
  },
  "installCommand": "uv venv && source .venv/bin/activate && uv pip install reportlab==4.0.4 Pillow>=9.0.0",
  "buildCommand": "source .venv/bin/activate && python3 -c 'import reportlab; print(\"Build complete - ReportLab installed\")'",
  "outputDirectory": "."
}
